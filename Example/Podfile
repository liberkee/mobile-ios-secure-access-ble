platform :ios, '9.0'
use_frameworks!

target 'SecureAccessBLE_Tests' do

  pod 'SwiftFormat/CLI'

  pod 'SecureAccessBLE', :path => '../'

  pod 'CommonUtils', :path => '../../mobile-ios-common-utils'
  
end
